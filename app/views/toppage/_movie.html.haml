%li
  -#= link_to item_path(item) do
    .movie
      - if movie.images.present?
        = image_tag movie.images[0].image.url, class: 'item-img',width: '100%',height: '100%'
      - else
        = image_tag "",class: 'item-img',width: '100%',height: '100%'
      -#.plice
        -#= "¥#{item.price}"
    .movie-name
      = movie.name